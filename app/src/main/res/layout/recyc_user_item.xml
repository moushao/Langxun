<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    android:id="@+id/layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="59dp"
    android:background="@color/back_normal"
    >


    <TextView
        android:id="@+id/back"
        android:layout_width="0dp"
        android:layout_height="59dp"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        android:background="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.545"/>

    <ImageView
        android:id="@+id/group_user_list_head"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="0dp"
        app:layout_constraintBottom_toBottomOf="@+id/back"
        app:layout_constraintLeft_toLeftOf="@+id/back"
        app:layout_constraintTop_toTopOf="@+id/back"
        app:srcCompat="@drawable/login_head"/>

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:text="张三"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@+id/back"
        app:layout_constraintLeft_toRightOf="@id/group_user_list_head"
        app:layout_constraintTop_toTopOf="@+id/back"
        />

    <com.langbai.tdhd.widget.smoothcheck.SmoothCheckBox
        android:id="@+id/check"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginRight="28dp"
        android:clickable="false"
        android:paddingBottom="10dp"
        app:color_checked="#149A45"
        app:layout_constraintBottom_toBottomOf="@+id/back"
        app:layout_constraintRight_toRightOf="@+id/back"
        app:layout_constraintTop_toTopOf="@+id/back"
        app:stroke_width="20dp"
        />

</android.support.constraint.ConstraintLayout>